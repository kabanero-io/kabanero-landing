apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: kabanero-landing-deployment
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: kabanero-landing
    spec:
      containers:
      - name: kabanero-landing
        image: @DOCKER_REGISTRY@/@NAMESPACE@/@IMAGE_NAME@:@IMAGE_TAG@
        ports:
          - containerPort: 9443
      imagePullSecrets:
      - name: regcred
---
kind: Service
apiVersion: v1
metadata:
  name: kabanero-landing-service
spec:
  ports:
    - port: 9443
      targetPort: 9443
      nodePort: 32000
      name: https
  type: NodePort
  selector:
    app: kabanero-landing